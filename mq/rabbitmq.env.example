# Copy the needed values into an uncommitted .env file or service environment.
# Never commit the actual username, password, or broker URL.
RABBITMQ_URL=
RABBITMQ_HEARTBEAT=30
RABBITMQ_BLOCKED_TIMEOUT=30
