# App Service

- `frontend/` contains the browser interface.
- `backend/` contains Django configuration, business services, persistence
  models and migrations, RabbitMQ coordination, and application logging.

The top-level `api/` folder is intentionally limited to the HTTP API boundary.
